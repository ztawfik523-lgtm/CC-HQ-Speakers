# M0.5 cleanup / redesign preparation

## Purpose

M0.5 prepares the M1A local-file prototype for the accepted asset/playback redesign without spending effort repairing prototype concepts which will be removed.

Prototype reference preserved unchanged:

`69e34a5346f6ce47580f49ed867c9951bfd338bc` (`codex/m1a-local-finite-media`)

Active M0.5 branch:

`codex/m0.5-cleanup-prep`

## Completed in this milestone

- created a separate cleanup/redesign branch from the frozen prototype reference;
- added deterministic provider cleanup for CC speaker block removal;
- added deterministic provider cleanup for server `LevelEvent.Unload`;
- added final provider cleanup on `ServerStoppedEvent`;
- documented why the weak Level-key cache alone cannot guarantee collection when values reference the same Level;
- replaced stale unresolved P0 D1-D4 guidance with the accepted architecture;
- rewrote `ROADMAP.md`, `ARCHITECTURE.md`, `CURRENT-STATE.md`, and `AGENTS.md` around the asset/playback/dynamic-renderer design;
- added CI verification that the packaged JAR contains `data/computercraft/lua/rom/modules/main/hqspeaker.lua`.

## Intentionally not repaired

The following M1A prototype concepts are kept only as working evidence until their replacement lands:

- fixed playback recipient set;
- server-push entire-file transfer;
- per-speaker media identity/lifetime;
- renderer observation timeout;
- client READY/STARTED/ENDED as canonical finite state authority;
- successful-renderer/anchor-style state;
- delete-on-initial-transfer behavior;
- expected group/tap-count finite synchronization.

Do not add incremental fixes to these unless a change is strictly required to keep the branch buildable while replacing them.

## Next implementation milestone

Start M1A/M1B from the accepted `docs/ROADMAP.md` order:

1. compatibility/output ownership boundary;
2. reusable finite media asset layer;
3. local CC file import onto that asset layer;
4. server metadata analysis and server-authoritative finite clock;
5. bounded client-pulled asset ranges and reusable client cache;
6. incremental decoder and dynamic positional renderers.

## Verification

M0.5 exit requires:

- NeoForge 21.1.247 build/test/package pass;
- NeoForge 21.1.248 build/test/package pass;
- packaged Lua ROM module check pass;
- final source review confirms no prototype finite architecture was accidentally promoted as the target;
- final lifecycle review confirms speaker removal, Level unload, and server shutdown all evict provider composites.
